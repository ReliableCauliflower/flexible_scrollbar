name: example
description: An example app for the flexible_scrollbar package.

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flexible_scrollbar:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
